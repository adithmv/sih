# PostgreSQL connection string
DATABASE_URL=postgres://user:password@host:5432/dbname
# JWT signing secret
JWT_SECRET=replace-with-a-long-random-string
# HTTP server port
PORT=5000
# Priority 1.2 doctor-verify placeholder auth — demo only, replace with real doctor accounts in production
DOCTOR_TOKEN=demo-doctor-key
